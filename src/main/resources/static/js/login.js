$(window).on({
   load: function() {
       // 회원가입
       $('#join').click(function() {
           alert("join");
       });

       // 로그인
       $('#login').click(function() {
           alert("login");
       });
   }
});