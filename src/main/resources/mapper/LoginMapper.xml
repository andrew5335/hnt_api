<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.andrew.hnt.api.mapper.LoginMapper">

    <select id="getUserList" parameterType="java.util.HashMap" resultType="com.andrew.hnt.api.model.UserInfo">
        select
            no
            , user_nm
            , user_tel
            , user_email
            , user_id
            , user_pass
            , user_grade
            , use_yn
            , del_yn
        from
            hnt_user
        where 1 = 1
        and use_yn = 'Y'
        and del_yn = 'N'
        order by no desc
    </select>

    <select id="getUserInfo" parameterType="java.util.HashMap" resultType="com.andrew.hnt.api.model.UserInfo">
        select
            no
                , user_nm
                , user_tel
                , user_email
                , user_id
                , user_pass
                , user_grade
                , use_yn
                , del_yn
        from
            hnt_user
        where 1 = 1
        and use_yn = 'Y'
        and del_yn = 'N'
        and no = #{no}
        and user_id = #{userId}
    </select>

    <insert id="insertUserInfo" parameterType="com.andrew.hnt.api.model.UserInfo">
        insert into hnt_user
            (user_nm, user_tel, user_email, user_id, user_pass, user_grade, use_yn, del_yn, inst_id, inst_dtm, mdf_id, mdf_dtm)
        values
            (#{userNm}, #{userTel}, #{userEmail}, #{userId}, #{userPass}, #{userGrade}, #{useYn}, #{delYn}, #{instId}, now(), #{mdfId}, now())
    </insert>

</mapper>